<footer class="footer">
<h4>About</h4>
<p>Listing articles and resources around crisis comms advice during the Coronavirus pandemic. The website is a static website optimised for heavy traffic and maximum resilience.</p>
    {% if meta.contact.email %}
        <p>Submit resources: <a href="mailto:{{ meta.contact.email }}">{{ meta.contact.email }}</a></p>
    {% endif %}
    {% if meta.contact.website %}
        <p>Get support: <a href="{{ meta.contact.website }}">{{ meta.contact.website }}</a></p>
    {% endif %}
</footer>
